<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC 
        "-//Hibernate/Hibernate Configuration DTD 5.3//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-5.3.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql:///info</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Manish@2005</property>

        <!-- JDBC connection pool (optional) -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
        
        <!-- ✅  HIKARICP CONNECTION POOL -->
        <!-- ============================= -->
        <property name="hibernate.connection.provider_class">
          org.hibernate.hikaricp.internal.HikariCPConnectionProvider</property>
        
        <property name="hibernate.hikari.minimumIdle">5</property>
        <property name="hibernate.hikari.maximumPoolSize">20</property>
        <property name="hibernate.hikari.idleTimeout">30000</property>
        <property name="hibernate.hikari.maxLifetime">600000</property>
        <property name="hibernate.hikari.connectionTimeout">20000</property>
        <property name="hibernate.hikari.poolName">MyHikariPool</property>

        <!-- Optional: extra tuning -->
        <property name="hibernate.hikari.autoCommit">true</property>
        <property name="hibernate.hikari.keepaliveTime">300000</property>

        <!-- ============================= -->
        <!-- ✅  HIBERNATE BEHAVIOR -->
        <!-- ============================= -->
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ============================= -->
        <!-- ✅  SECOND-LEVEL CACHE (OPTIONAL) -->
        <!-- ============================= -->
        <property name="hibernate.cache.use_second_level_cache">false</property>
         


        <!-- Show SQL in console -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Automatically create or update database schema -->
        <property name="hbm2ddl.auto">update</property>

        

        <!-- OR specify mapping XML files -->
        <!-- <mapping resource="student.hbm.xml"/> -->

    </session-factory>
</hibernate-configuration>
